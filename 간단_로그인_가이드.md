# 🎯 Supabase 회원가입/로그인 - 초간단 가이드

## ✅ 완성된 기능

- ✅ Supabase 회원가입 API
- ✅ Supabase 로그인 API  
- ✅ 회원가입 페이지 (`/signup`)
- ✅ 로그인 페이지 (`/login`)
- ✅ 커스텀 훅 없이 간단한 코드
- ✅ useState만 사용하여 구현

## 🚀 바로 테스트하기

### 1단계: 서버 실행
```bash
npm run dev
```

### 2단계: 회원가입
1. 브라우저에서 `http://localhost:3000/signup` 접속
2. 정보 입력:
   - 이름: 홍길동
   - 이메일: test@example.com
   - 전화번호: 010-1234-5678
   - 비밀번호: test1234
   - 비밀번호 확인: test1234
3. "회원가입" 버튼 클릭
4. 자동으로 로그인 페이지로 이동

### 3단계: 로그인
1. 이메일: test@example.com
2. 비밀번호: test1234
3. "로그인" 버튼 클릭
4. 홈페이지로 이동

## ⚙️ 이메일 인증 끄기 (중요!)

Supabase는 기본적으로 이메일 인증을 요구합니다. 테스트를 위해 끄세요:

1. https://supabase.com/dashboard 접속
2. 프로젝트 선택
3. **Authentication** 메뉴 클릭
4. **Providers** 탭 클릭
5. **Email** 클릭
6. **Confirm email** 토글을 **OFF**로 변경
7. **Save** 버튼 클릭

✅ 이제 이메일 인증 없이 바로 회원가입/로그인 가능합니다!

## 📁 코드 구조

### API (서버)
```
app/api/auth/signup/route.ts  - 회원가입 API (Supabase)
app/api/auth/login/route.ts   - 로그인 API (Supabase)
```

### 페이지 (클라이언트)
```
app/signup/page.tsx  - 회원가입 페이지 (useState만 사용)
app/login/page.tsx   - 로그인 페이지 (useState만 사용)
```

## 💡 코드 특징

### ✅ 간단함
- 커스텀 훅 없음
- useState만 사용
- 복잡한 상태관리 없음
- 직관적인 코드

### ✅ Supabase 자동 처리
- 비밀번호 자동 암호화
- 세션 자동 관리
- 토큰 자동 생성
- 보안 처리

## 🔍 Supabase에서 사용자 확인

1. Supabase 대시보드 접속
2. **Authentication** > **Users** 메뉴
3. 가입된 사용자 목록 확인

## 📝 주요 코드 설명

### 회원가입 API
```typescript
// Supabase의 signUp 메서드 사용
const { data, error } = await supabase.auth.signUp({
  email,
  password,
  options: {
    data: { name, phone }  // 추가 정보 저장
  }
})
```

### 로그인 API
```typescript
// Supabase의 signInWithPassword 메서드 사용
const { data, error } = await supabase.auth.signInWithPassword({
  email,
  password
})
```

### 회원가입 페이지
```typescript
// useState만 사용한 간단한 폼
const [email, setEmail] = useState('')
const [password, setPassword] = useState('')
const [error, setError] = useState('')

// API 호출
const response = await fetch('/api/auth/signup', {
  method: 'POST',
  body: JSON.stringify({ email, password, name, phone })
})
```

## ❓ 문제 해결

**Q: 로그인이 안 돼요!**
```
→ Supabase에서 "Confirm email" 설정을 OFF로 변경하세요
→ 회원가입 시 사용한 이메일/비밀번호가 맞는지 확인하세요
```

**Q: "이미 사용 중인 이메일입니다" 에러가 나와요**
```
→ 이미 가입한 이메일입니다
→ 로그인 페이지에서 바로 로그인하세요
→ 또는 다른 이메일로 가입하세요
```

**Q: 비밀번호는 안전한가요?**
```
→ 네! Supabase가 자동으로 암호화합니다
→ 평문으로 저장되지 않습니다
→ bcrypt 알고리즘 사용
```

## 🎉 완료!

이제 간단한 회원가입/로그인 시스템을 사용할 수 있습니다!

- 커스텀 훅 없음 ✅
- 복잡한 상태관리 없음 ✅
- Supabase 자동 처리 ✅
- 깔끔한 UI ✅

