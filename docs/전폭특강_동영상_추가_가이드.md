# 전폭특강 동영상 추가 가이드

## 📹 동영상을 갤러리에 추가하는 방법

전폭특강 페이지의 사진 갤러리에 6분 30초 동영상을 추가할 수 있습니다!

### 방법 1: 로컬 동영상 파일 사용 (추천)

#### 1단계: 동영상 파일 저장

동영상 파일을 다음 위치에 저장하세요:

```
public/images/gospel-training-video.mp4
```

**지원하는 동영상 포맷:**
- `.mp4` (추천)
- `.webm`
- `.mov`
- `.avi`
- `.m4v`

**파일 저장 방법:**
1. 동영상 파일을 준비
2. 파일명을 `gospel-training-video.mp4`로 변경
3. `public/images/` 폴더에 복사

#### 2단계: Supabase에서 SQL 실행

**Supabase SQL Editor**에서 아래 SQL을 실행하세요:

```sql
-- 전폭특강 갤러리에 동영상 추가
-- Task ID: 00113bf0-ec8b-4733-888d-74d13fe8e192

-- 1단계: 현재 전폭특강 데이터 확인
SELECT 
  id, 
  title, 
  title_zh,
  image_url,
  images,
  array_length(images, 1) as image_count
FROM tasks 
WHERE id = '00113bf0-ec8b-4733-888d-74d13fe8e192';

-- 2단계: 갤러리에 동영상 추가 (기존 이미지 유지하면서)
UPDATE tasks
SET 
  images = array_append(images, '/images/gospel-training-video.mp4'),
  updated_at = NOW()
WHERE id = '00113bf0-ec8b-4733-888d-74d13fe8e192';

-- 3단계: 업데이트 결과 확인
SELECT 
  id, 
  title, 
  images,
  array_length(images, 1) as image_count,
  updated_at
FROM tasks 
WHERE id = '00113bf0-ec8b-4733-888d-74d13fe8e192';
```

### 방법 2: 유튜브 동영상 사용

유튜브에 동영상을 업로드한 경우:

#### 1단계: 유튜브 Embed URL 얻기

1. 유튜브에서 동영상 업로드
2. 동영상 페이지에서 **공유** → **퍼가기** 클릭
3. Embed URL 복사 (예: `https://www.youtube.com/embed/VIDEO_ID`)

#### 2단계: Supabase에서 SQL 실행

```sql
-- 전폭특강 갤러리에 유튜브 동영상 추가
UPDATE tasks
SET 
  images = array_append(images, 'https://www.youtube.com/embed/YOUR_VIDEO_ID'),
  updated_at = NOW()
WHERE id = '00113bf0-ec8b-4733-888d-74d13fe8e192';
```

## 📝 전체 갤러리 재설정 (선택사항)

기존 이미지와 동영상을 모두 재설정하려면:

```sql
-- 전폭특강 갤러리 전체 재설정
UPDATE tasks
SET 
  images = ARRAY[
    '/images/gospel-training-poster-1.jpg',
    '/images/gospel-training-poster-2.jpg',
    '/images/gospel-training-poster-3.jpg',
    '/images/gospel-training-video.mp4'  -- 동영상 추가
  ],
  updated_at = NOW()
WHERE id = '00113bf0-ec8b-4733-888d-74d13fe8e192';
```

## ✅ 웹사이트에서 확인

1. Git commit & push (Vercel 자동 배포)
2. 웹사이트 접속: https://gospel-meeting.vercel.app/tasks/00113bf0-ec8b-4733-888d-74d13fe8e192
3. 페이지 새로고침 (Ctrl+F5)
4. 사진 갤러리에서 동영상이 재생되는지 확인

## 🎬 동영상 표시 방식

- **로컬 동영상**: HTML5 video 태그로 재생 (플레이어 컨트롤 포함)
- **유튜브 동영상**: iframe으로 embed (유튜브 플레이어)
- **배지**: 동영상에는 🎥 빨간 배지가 표시됩니다

## 🔍 문제 해결

### 동영상이 표시되지 않는 경우:

1. **파일 경로 확인**
   ```bash
   # Windows PowerShell
   Get-ChildItem public\images\gospel-training*
   ```

2. **파일 크기 확인**
   - Git은 기본적으로 100MB 이상 파일을 업로드하지 못합니다
   - 큰 동영상은 유튜브를 사용하세요

3. **Git LFS 사용 (대용량 파일)**
   ```bash
   git lfs install
   git lfs track "*.mp4"
   git add .gitattributes
   ```

4. **Vercel 배포 확인**
   - https://vercel.com/dashboard 에서 배포 상태 확인
   - 빌드 로그에서 에러 확인

### 동영상 재생이 느린 경우:

- 동영상 용량을 줄이거나
- 유튜브를 사용하세요 (추천)

## 📊 파일 크기 권장사항

- **로컬 동영상**: 50MB 이하 권장
- **6분 30초 동영상**: 720p로 인코딩하면 약 30-50MB
- **대용량 동영상**: 유튜브 사용 필수!

## 🚀 Git 커밋 & 배포

```bash
# 1. 파일 추가
git add public/images/gospel-training-video.mp4

# 2. 커밋
git commit -m "전폭특강 동영상 추가"

# 3. Push (Vercel 자동 배포)
git push
```

배포 완료까지 약 1-2분 소요됩니다! 🎥✨

