# 미용팀 메인 이미지 추가 가이드

## 📸 이미지 추가 절차

### 1단계: 이미지 파일 저장

첨부하신 사진(체육관에서 많은 사람들이 모여있는 사진)을 다음 위치에 저장하세요:

```
public/images/beauty-team-main.jpg
```

**저장 방법:**
1. 첨부하신 사진을 다운로드
2. 파일명을 `beauty-team-main.jpg`로 변경
3. `public/images/` 폴더에 복사

### 2단계: Supabase에서 SQL 실행

**Supabase SQL Editor**에서 아래 SQL을 실행하세요:

```sql
-- 미용팀 메인 이미지 업데이트
-- Task ID: 621b6db4-996e-4f91-b619-0e69c9b665e0

-- 1단계: 현재 미용팀 데이터 확인
SELECT 
  id, 
  title, 
  title_zh,
  image_url,
  images,
  updated_at
FROM tasks 
WHERE id = '621b6db4-996e-4f91-b619-0e69c9b665e0';

-- 2단계: 미용팀 메인 이미지 업데이트
UPDATE tasks
SET 
  image_url = '/images/beauty-team-main.jpg',
  updated_at = NOW()
WHERE id = '621b6db4-996e-4f91-b619-0e69c9b665e0';

-- 3단계: 업데이트 결과 확인
SELECT 
  id, 
  title, 
  title_zh,
  image_url,
  images,
  updated_at
FROM tasks 
WHERE id = '621b6db4-996e-4f91-b619-0e69c9b665e0';
```

### 3단계: 웹사이트에서 확인

1. 웹사이트 새로고침 (Ctrl+F5)
2. **/tasks** 페이지에서 미용팀 카드에 이미지가 표시되는지 확인
3. 미용팀 카드를 클릭하면 상세 페이지로 이동합니다

## 📝 갤러리에 여러 사진 추가하기 (선택사항)

상세 페이지 갤러리에 여러 장의 사진을 추가하려면:

1. **파일 저장**:
   - `beauty-team-main.jpg` (메인 이미지)
   - `beauty-team-1.jpg` (갤러리 1)
   - `beauty-team-2.jpg` (갤러리 2)
   - `beauty-team-3.jpg` (갤러리 3)
   - ...

2. **SQL 업데이트** (메인 이미지 + 갤러리):

```sql
UPDATE tasks
SET 
  image_url = '/images/beauty-team-main.jpg',
  images = ARRAY[
    '/images/beauty-team-main.jpg',
    '/images/beauty-team-1.jpg',
    '/images/beauty-team-2.jpg',
    '/images/beauty-team-3.jpg'
  ],
  updated_at = NOW()
WHERE id = '621b6db4-996e-4f91-b619-0e69c9b665e0';
```

**설명**:
- `image_url`: /tasks 페이지 카드에 표시되는 메인 이미지
- `images`: 상세 페이지 갤러리에 표시되는 이미지 배열

## ✅ 완료!

이제 https://gospel-meeting.vercel.app/tasks 페이지의 미용팀 카드에 사진이 표시됩니다! 💇‍♀️

## 🔍 문제 해결

### 이미지가 표시되지 않는 경우:
1. 파일 경로가 정확한지 확인 (`public/images/beauty-team-main.jpg`)
2. Vercel에 배포 필요 (Git push 후 자동 배포)
3. 브라우저 캐시 초기화 (Ctrl+Shift+Delete)

### 이미지 파일명 확인:
```bash
# Windows PowerShell
Get-ChildItem public\images\beauty*

# 또는 파일 탐색기에서 직접 확인
```

