-- 중보기도팀 이미지 즉시 업데이트
-- 이 SQL을 Supabase SQL Editor에서 반드시 실행하세요!

-- ============================================
-- 실행 전 확인
-- ============================================
SELECT 
  id, 
  title,
  image_url as "업데이트 전 image_url"
FROM tasks 
WHERE title = '중보기도팀';

-- ============================================
-- 🔥 이미지 업데이트 실행 🔥
-- ============================================
UPDATE tasks
SET image_url = '/images/prayer-team-1.jpg.jpg'
WHERE title = '중보기도팀';

-- ============================================
-- 실행 후 확인
-- ============================================
SELECT 
  id, 
  title,
  image_url as "업데이트 후 image_url"
FROM tasks 
WHERE title = '중보기도팀';

-- ============================================
-- 업데이트된 행 수 확인
-- ============================================
-- 위의 UPDATE가 "1 row affected" 또는 "UPDATE 1"로 표시되어야 합니다

